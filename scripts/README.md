# Scripts

Scripts utilitários do repositório.

- `check-env.sh` — inspeciona ferramentas locais
- `bootstrap-local.sh` — prepara toolchain local/devcontainer de forma opcional
- `validate.sh` — executa validações estáticas do repo
